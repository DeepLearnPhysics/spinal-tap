apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spinal-tap

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
- pvc.yaml

labels:
- includeSelectors: true
  pairs:
    app: spinal-tap
